services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: haoflow-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://app.haoflow.net
    restart: unless-stopped